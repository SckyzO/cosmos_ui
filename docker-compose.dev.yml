services:
  demo:
    build:
      context: ./demo
      dockerfile: Dockerfile
    ports:
      - "5174:5173"
    volumes:
      - ./demo:/app
      - ./packages/react/src:/app/src/cosmos
      - ./styles:/app/styles
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
